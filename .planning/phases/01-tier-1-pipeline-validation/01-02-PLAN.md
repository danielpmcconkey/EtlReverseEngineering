---
phase: 01-tier-1-pipeline-validation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - job-confs/compliance_resolution_time_re.json
  - jobs/ComplianceResolutionTime/BRD.md
  - jobs/ComplianceResolutionTime/FSD.md
  - jobs/ComplianceResolutionTime/test-strategy.md
  - jobs/ComplianceResolutionTime/output-manifesto.md
  - jobs/ComplianceResolutionTime/anti-pattern-assessment.md
autonomous: true
requirements:
  - DELIV-01
  - DELIV-02
  - DELIV-03
  - DELIV-04
  - DELIV-05
  - DELIV-06
  - DELIV-07
  - DELIV-08
  - PROC-01
  - PROC-02
  - ANTI-01
  - ANTI-02
  - ANTI-03

must_haves:
  truths:
    - "ComplianceResolutionTime_RE produces byte-identical output to V1 across all 92 dates"
    - "Trailer line (TRAILER|{row_count}|{date}) matches V1 exactly"
    - "Complete doc set follows templates established by BranchDirectory"
    - "Anti-pattern assessment documents AP4 (customer_id unused) and AP8 (JOIN ON 1=1 hack, unused ROW_NUMBER)"
    - "FSD documents integer division behavior in avg_resolution_days (AP5 documentation)"
    - "Full 13-step workflow executes with zero human input"
  artifacts:
    - path: "job-confs/compliance_resolution_time_re.json"
      provides: "RE job conf with AP4/AP8 remediations and trailerFormat"
      contains: "{ETL_RE_OUTPUT}"
    - path: "jobs/ComplianceResolutionTime/BRD.md"
      provides: "BRD following template from BranchDirectory"
      min_lines: 50
    - path: "jobs/ComplianceResolutionTime/FSD.md"
      provides: "FSD with traceability and AP documentation"
      min_lines: 40
    - path: "jobs/ComplianceResolutionTime/test-strategy.md"
      provides: "Test strategy with Proofmark trailer handling"
      min_lines: 20
    - path: "jobs/ComplianceResolutionTime/output-manifesto.md"
      provides: "Output listing noting trailer format"
      min_lines: 10
    - path: "jobs/ComplianceResolutionTime/anti-pattern-assessment.md"
      provides: "AP1-AP10 assessment with AP4, AP5, AP8 findings"
      min_lines: 30
  key_links:
    - from: "job-confs/compliance_resolution_time_re.json"
      to: "control.jobs"
      via: "DB registration with matching jobName"
      pattern: "ComplianceResolutionTime_RE"
    - from: "job-confs/compliance_resolution_time_re.json"
      to: "V1 trailer format"
      via: "trailerFormat field must match V1 exactly"
      pattern: "TRAILER.*row_count.*date"
    - from: "control.proofmark_test_queue"
      to: "proofmark-configs/ComplianceResolutionTime.yaml"
      via: "config_path with trailer_rows: 1"
      pattern: "PASS.*92"
---

<objective>
Complete the full 13-step RE workflow for ComplianceResolutionTime -- the second job. This job adds trailer handling complexity and has more anti-pattern findings (AP4, AP5, AP8) than BranchDirectory. Apply templates from Plan 01 with refinements.

Purpose: Validates that the document templates scale to a slightly more complex job. Introduces trailer handling as a pattern for future jobs. Tests AP4 remediation (removing unused columns from DataSourcing).

Output: Working ComplianceResolutionTime_RE with 92/92 Proofmark PASS and complete doc set.
</objective>

<execution_context>
@/home/sandbox/.claude/get-shit-done/workflows/execute-plan.md
@/home/sandbox/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-tier-1-pipeline-validation/01-RESEARCH.md
@.planning/phases/01-tier-1-pipeline-validation/01-01-SUMMARY.md

Source V1 job conf:
- /workspace/MockEtlFramework/JobExecutor/Jobs/compliance_resolution_time.json

RE Blueprint:
- /workspace/AtcStrategy/POC5/re-blueprint.md

BranchDirectory docs (template reference):
- /workspace/EtlReverseEngineering/jobs/BranchDirectory/BRD.md
- /workspace/EtlReverseEngineering/jobs/BranchDirectory/FSD.md

<interfaces>
<!-- V1 ComplianceResolutionTime job conf structure -->
DataSourcing: datalake.compliance_events (event_id, customer_id, event_type, event_date, status, review_date)
Transformation SQL: CTE "resolved" filters Cleared events, calculates days_to_resolve via julianday(),
  then aggregates by event_type with cartesian JOIN compliance_events ON 1=1 to get ifw_effective_date
CsvFileWriter: header=true, trailerFormat="TRAILER|{row_count}|{date}", LF, Overwrite

Output columns: event_type, resolved_count, total_days, avg_resolution_days, ifw_effective_date, etl_effective_date
Row count: 5 data rows + 1 trailer per date

Anti-pattern findings:
- AP4: customer_id sourced but never used in output or SQL logic
- AP5: Integer division in avg_resolution_days (CAST to INTEGER / CAST to INTEGER) -- document, do not change
- AP8: JOIN compliance_events ON 1=1 is a hack to get ifw_effective_date (available directly on resolved CTE rows); ROW_NUMBER computed but never used for filtering

Path tokens: same as Plan 01
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ComplianceResolutionTime documentation (BRD, FSD, test strategy, manifesto, AP assessment)</name>
  <files>
    jobs/ComplianceResolutionTime/BRD.md
    jobs/ComplianceResolutionTime/FSD.md
    jobs/ComplianceResolutionTime/test-strategy.md
    jobs/ComplianceResolutionTime/output-manifesto.md
    jobs/ComplianceResolutionTime/anti-pattern-assessment.md
  </files>
  <action>
Follow the templates established in Plan 01 (read BranchDirectory docs for format reference).

**BRD** (`jobs/ComplianceResolutionTime/BRD.md`):
- Numbered requirements (BRD-CRT-001, BRD-CRT-002, etc.)
- Data source: `datalake.compliance_events` (6 columns: event_id, customer_id, event_type, event_date, status, review_date)
- Business rules: Filter to status='Cleared' AND review_date IS NOT NULL, calculate days_to_resolve via julianday(review_date) - julianday(event_date), aggregate by event_type
- Output: 5 data rows per date, with trailer `TRAILER|{row_count}|{date}`
- Evidence citing V1 SQL for each requirement

**FSD** (`jobs/ComplianceResolutionTime/FSD.md`):
- Numbered specs (FSD-CRT-001, etc.) traceable to BRD numbers
- Document AP remediations:
  - AP4: Remove `customer_id` from DataSourcing columns (sourced but never used)
  - AP8: Replace `JOIN compliance_events ON 1=1` hack with direct access to `ifw_effective_date` on the resolved CTE. Remove unused ROW_NUMBER.
  - AP5: Document that integer division for avg_resolution_days is V1 behavior, NOT changed (output must match)
- Document: No external module needed (DELIV-05 trivially satisfied)
- CRITICAL: The trailerFormat must be included in the CsvFileWriter: `"trailerFormat": "TRAILER|{row_count}|{date}"`

**Test strategy** (`jobs/ComplianceResolutionTime/test-strategy.md`):
- Same format as BranchDirectory
- Note: Proofmark config uses trailer_rows: 1 (already created in Plan 01)

**Output manifesto** (`jobs/ComplianceResolutionTime/output-manifesto.md`):
- Single output: `compliance_resolution_time.csv`
- Path: `{ETL_RE_OUTPUT}/compliance_resolution_time/compliance_resolution_time/{date}/compliance_resolution_time.csv`
- Format: CSV, header, LF line endings, 5 data rows + 1 trailer, 6 columns

**Anti-pattern assessment** (`jobs/ComplianceResolutionTime/anti-pattern-assessment.md`):
- AP1: Clean (single source used)
- AP2: N/A
- AP3: N/A (no external module)
- AP4: YES -- `customer_id` sourced but never referenced. REMEDIATE: remove from DataSourcing columns.
- AP5: Document integer division behavior. NOT remediated (output must match).
- AP6: N/A (no C# code)
- AP7: Clean
- AP8: YES -- (1) `JOIN compliance_events ON 1=1` cartesian join hack to get ifw_effective_date, (2) ROW_NUMBER computed but never filtered on. REMEDIATE: access ifw_effective_date directly from resolved CTE, remove ROW_NUMBER.
- AP9: Clean
- AP10: Clean
  </action>
  <verify>
    <automated>ls /workspace/EtlReverseEngineering/jobs/ComplianceResolutionTime/BRD.md /workspace/EtlReverseEngineering/jobs/ComplianceResolutionTime/FSD.md /workspace/EtlReverseEngineering/jobs/ComplianceResolutionTime/test-strategy.md /workspace/EtlReverseEngineering/jobs/ComplianceResolutionTime/output-manifesto.md /workspace/EtlReverseEngineering/jobs/ComplianceResolutionTime/anti-pattern-assessment.md 2>&1</automated>
  </verify>
  <done>All 5 doc artifacts exist following BranchDirectory template format. BRD has numbered requirements with evidence. FSD has traceability and documents AP4/AP5/AP8. Anti-pattern assessment covers all 10 APs.</done>
</task>

<task type="auto">
  <name>Task 2: ComplianceResolutionTime job conf + execution + 92/92 PASS</name>
  <files>
    job-confs/compliance_resolution_time_re.json
  </files>
  <action>
**Write RE job conf** (`job-confs/compliance_resolution_time_re.json`):
```json
{
  "jobName": "ComplianceResolutionTime_RE",
  "firstEffectiveDate": "2024-10-01",
  "modules": [
    {
      "type": "DataSourcing",
      "resultName": "compliance_events",
      "schema": "datalake",
      "table": "compliance_events",
      "columns": ["event_id", "event_type", "event_date", "status", "review_date"]
    },
    {
      "type": "Transformation",
      "resultName": "resolution_stats",
      "sql": "<REMEDIATED SQL -- Remove JOIN ON 1=1 hack, remove unused ROW_NUMBER. Access ifw_effective_date directly from compliance_events in the CTE. Keep integer division for avg_resolution_days. Output columns MUST be: event_type, resolved_count, total_days, avg_resolution_days, ifw_effective_date. Do NOT add or remove columns.>"
    },
    {
      "type": "CsvFileWriter",
      "source": "resolution_stats",
      "includeHeader": true,
      "trailerFormat": "TRAILER|{row_count}|{date}",
      "writeMode": "Overwrite",
      "lineEnding": "LF",
      "outputDirectory": "{ETL_RE_OUTPUT}",
      "jobDirName": "compliance_resolution_time",
      "fileName": "compliance_resolution_time.csv",
      "outputTableDirName": "compliance_resolution_time"
    }
  ]
}
```

AP4 remediation: `customer_id` removed from DataSourcing columns.
AP8 remediation: SQL simplified to remove cartesian join and unused ROW_NUMBER.
CRITICAL: `trailerFormat` MUST be present and match V1 exactly. `lineEnding` MUST be `LF`.

**Register, execute, verify** (Steps 9-13 from workflow):
- Register in control.jobs with job_name = 'ComplianceResolutionTime_RE'
- Queue 92 dates in control.task_queue
- Verify all Succeeded
- Queue 92 Proofmark comparisons (use ComplianceResolutionTime.yaml config, which has trailer_rows: 1)
- Verify 92/92 PASS

Proofmark queue SQL:
```sql
INSERT INTO control.proofmark_test_queue (config_path, lhs_path, rhs_path, job_key, date_key)
SELECT
  '{ETL_RE_ROOT}/EtlReverseEngineering/proofmark-configs/ComplianceResolutionTime.yaml',
  '{ETL_ROOT}/Output/curated/compliance_resolution_time/compliance_resolution_time/' || to_char(d.dt, 'YYYY-MM-DD') || '/compliance_resolution_time.csv',
  '{ETL_RE_OUTPUT}/compliance_resolution_time/compliance_resolution_time/' || to_char(d.dt, 'YYYY-MM-DD') || '/compliance_resolution_time.csv',
  'ComplianceResolutionTime',
  d.dt::date
FROM generate_series('2024-10-01'::date, '2024-12-31'::date, '1 day') d(dt);
```

On failure: triage, RCA, fix SQL, reset failed tasks, retry (max 5). Common issue: integer division producing different results -- compare V1 output byte-by-byte.
  </action>
  <verify>
    <automated>PGPASSWORD=claude psql -h 172.18.0.1 -U claude -d atc -t -c "SELECT CASE WHEN COUNT(*) = 92 AND COUNT(*) FILTER (WHERE result = 'PASS') = 92 THEN 'OK: 92/92 PASS' ELSE 'FAIL: ' || COUNT(*) FILTER (WHERE result = 'PASS') || '/92 PASS' END FROM control.proofmark_test_queue WHERE job_key = 'ComplianceResolutionTime';"</automated>
  </verify>
  <done>ComplianceResolutionTime_RE produces 92/92 Proofmark PASS. Job conf uses {ETL_RE_OUTPUT}, remediates AP4 (removed customer_id) and AP8 (removed cartesian join hack and unused ROW_NUMBER). Trailer format matches V1.</done>
</task>

</tasks>

<verification>
1. ComplianceResolutionTime 92/92 PASS: `PGPASSWORD=claude psql -h 172.18.0.1 -U claude -d atc -c "SELECT result, COUNT(*) FROM control.proofmark_test_queue WHERE job_key = 'ComplianceResolutionTime' GROUP BY result;"`
2. All doc artifacts exist: `ls jobs/ComplianceResolutionTime/`
3. Job conf has trailerFormat: `grep trailerFormat job-confs/compliance_resolution_time_re.json`
4. Job conf uses correct output dir: `grep ETL_RE_OUTPUT job-confs/compliance_resolution_time_re.json`
5. Cumulative status: `PGPASSWORD=claude psql -h 172.18.0.1 -U claude -d atc -c "SELECT job_key, result, COUNT(*) FROM control.proofmark_test_queue GROUP BY job_key, result ORDER BY job_key, result;"`
</verification>

<success_criteria>
- ComplianceResolutionTime_RE: 92/92 Proofmark PASS
- Complete doc set following BranchDirectory templates
- AP4/AP5/AP8 assessed and documented; AP4 and AP8 remediated in job conf
- Trailer handling validated end-to-end
- Zero human input during execution
</success_criteria>

<output>
After completion, create `.planning/phases/01-tier-1-pipeline-validation/01-02-SUMMARY.md`

Include in summary:
- Template refinements from BranchDirectory experience
- Trailer handling pattern for future jobs
- AP4 remediation pattern (removing unused DataSourcing columns)
- Any issues encountered and how they were resolved
</output>
