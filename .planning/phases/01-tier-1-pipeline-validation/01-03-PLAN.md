---
phase: 01-tier-1-pipeline-validation
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - job-confs/overdraft_fee_summary_re.json
  - jobs/OverdraftFeeSummary/BRD.md
  - jobs/OverdraftFeeSummary/FSD.md
  - jobs/OverdraftFeeSummary/test-strategy.md
  - jobs/OverdraftFeeSummary/output-manifesto.md
  - jobs/OverdraftFeeSummary/anti-pattern-assessment.md
autonomous: true
requirements:
  - DELIV-01
  - DELIV-02
  - DELIV-03
  - DELIV-04
  - DELIV-05
  - DELIV-06
  - DELIV-07
  - DELIV-08
  - PROC-01
  - PROC-02
  - PROC-03
  - ANTI-01
  - ANTI-02
  - ANTI-03
  - COMP-01

must_haves:
  truths:
    - "OverdraftFeeSummary_RE produces byte-identical output to V1 across all 92 dates"
    - "Complete doc set follows refined templates from jobs 1 and 2"
    - "Anti-pattern assessment documents AP4 (3 unused columns) and AP8 (dead ROW_NUMBER)"
    - "All 3 Tier 1 jobs show 92/92 Proofmark PASS (COMP-01)"
    - "Lessons learned from Tier 1 are captured for Tier 2 (PROC-03)"
    - "Full 13-step workflow executes with zero human input across all 3 jobs"
  artifacts:
    - path: "job-confs/overdraft_fee_summary_re.json"
      provides: "RE job conf with AP4/AP8 remediations"
      contains: "{ETL_RE_OUTPUT}"
    - path: "jobs/OverdraftFeeSummary/BRD.md"
      provides: "BRD following refined template"
      min_lines: 50
    - path: "jobs/OverdraftFeeSummary/FSD.md"
      provides: "FSD with traceability and AP documentation"
      min_lines: 40
    - path: "jobs/OverdraftFeeSummary/test-strategy.md"
      provides: "Test strategy"
      min_lines: 20
    - path: "jobs/OverdraftFeeSummary/output-manifesto.md"
      provides: "Output listing"
      min_lines: 10
    - path: "jobs/OverdraftFeeSummary/anti-pattern-assessment.md"
      provides: "AP1-AP10 assessment with AP4 and AP8 findings"
      min_lines: 30
  key_links:
    - from: "job-confs/overdraft_fee_summary_re.json"
      to: "control.jobs"
      via: "DB registration with matching jobName"
      pattern: "OverdraftFeeSummary_RE"
    - from: "control.proofmark_test_queue"
      to: "3 job_keys all PASS"
      via: "Full suite query shows 3x92 = 276 PASS"
      pattern: "276.*PASS"
---

<objective>
Complete the full 13-step RE workflow for OverdraftFeeSummary (third and final Tier 1 job), then verify all 3 jobs pass and capture lessons learned for Tier 2. This plan closes COMP-01 and PROC-03.

Purpose: Final Tier 1 job validates the refined workflow. Most importantly, this plan captures iterative learning -- what worked, what broke, what templates need updating -- so Tier 2 can hit the ground running.

Output: Working OverdraftFeeSummary_RE with 92/92 PASS, all 3 jobs confirmed complete, lessons learned documented.
</objective>

<execution_context>
@/home/sandbox/.claude/get-shit-done/workflows/execute-plan.md
@/home/sandbox/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-tier-1-pipeline-validation/01-RESEARCH.md
@.planning/phases/01-tier-1-pipeline-validation/01-01-SUMMARY.md
@.planning/phases/01-tier-1-pipeline-validation/01-02-SUMMARY.md

Source V1 job conf:
- /workspace/MockEtlFramework/JobExecutor/Jobs/overdraft_fee_summary.json

RE Blueprint:
- /workspace/AtcStrategy/POC5/re-blueprint.md

Prior job docs (template reference -- use refined versions from ComplianceResolutionTime):
- /workspace/EtlReverseEngineering/jobs/ComplianceResolutionTime/BRD.md
- /workspace/EtlReverseEngineering/jobs/ComplianceResolutionTime/FSD.md

<interfaces>
<!-- V1 OverdraftFeeSummary job conf structure -->
DataSourcing: datalake.overdraft_events (overdraft_id, account_id, customer_id, overdraft_amount, fee_amount, fee_waived, event_timestamp)
Transformation SQL: CTE "all_events" with ROW_NUMBER (unused), then GROUP BY fee_waived with SUM, COUNT, AVG, ORDER BY fee_waived
CsvFileWriter: header=true, LF, Overwrite, no trailer

Output columns: fee_waived, total_fees, event_count, avg_fee, ifw_effective_date, etl_effective_date
Row count: 2 data rows per date (fee_waived=0 and fee_waived=1)

Anti-pattern findings:
- AP4: YES -- account_id, customer_id, event_timestamp sourced but never referenced. overdraft_id only used in dead ROW_NUMBER ORDER BY.
- AP8: YES -- ROW_NUMBER computed in CTE but never filtered on (rn never referenced in outer query)

Path tokens: same as Plans 01/02
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OverdraftFeeSummary full 13-step workflow (docs + job conf + execution + PASS)</name>
  <files>
    jobs/OverdraftFeeSummary/BRD.md
    jobs/OverdraftFeeSummary/FSD.md
    jobs/OverdraftFeeSummary/test-strategy.md
    jobs/OverdraftFeeSummary/output-manifesto.md
    jobs/OverdraftFeeSummary/anti-pattern-assessment.md
    job-confs/overdraft_fee_summary_re.json
  </files>
  <action>
Execute the full 13-step workflow for OverdraftFeeSummary. By now the templates are proven -- apply them efficiently.

**Steps 1-2: Read V1 conf and check output**
- Read `/workspace/MockEtlFramework/JobExecutor/Jobs/overdraft_fee_summary.json`
- Sample V1 output at 2024-10-01: 2 data rows, header, LF, no trailer, 6 columns

**Step 3: Write BRD** (`jobs/OverdraftFeeSummary/BRD.md`)
- Numbered requirements (BRD-OFS-001, etc.)
- Data source: `datalake.overdraft_events` (7 columns)
- Business rules: GROUP BY fee_waived, aggregate with SUM(fee_amount), COUNT(*), AVG(fee_amount), ROUND to 2 decimal places
- Output: 2 data rows per date, no trailer

**Step 4: Write FSD** (`jobs/OverdraftFeeSummary/FSD.md`)
- Numbered specs traceable to BRD
- Document AP remediations:
  - AP4: Remove `account_id`, `customer_id`, `event_timestamp` from DataSourcing columns. Remove `overdraft_id` too (only used in dead ROW_NUMBER).
  - AP8: Remove dead ROW_NUMBER from CTE (never filtered on)
- Note: Only `fee_amount`, `fee_waived` needed from DataSourcing (plus framework-added columns)
- No external module needed

**Step 5: Write test strategy** (`jobs/OverdraftFeeSummary/test-strategy.md`)

**Step 6: Write anti-pattern assessment** (`jobs/OverdraftFeeSummary/anti-pattern-assessment.md`)
- AP4: YES -- 4 columns removable (account_id, customer_id, event_timestamp, overdraft_id)
- AP8: YES -- dead ROW_NUMBER
- All others: Clean or N/A

**Step 7: Write output manifesto** (`jobs/OverdraftFeeSummary/output-manifesto.md`)
- Single output: `overdraft_fee_summary.csv`
- Path: `{ETL_RE_OUTPUT}/overdraft_fee_summary/overdraft_fee_summary/{date}/overdraft_fee_summary.csv`
- Format: CSV, header, LF, 2 data rows, 6 columns, no trailer

**Step 8: Write RE job conf** (`job-confs/overdraft_fee_summary_re.json`):
```json
{
  "jobName": "OverdraftFeeSummary_RE",
  "firstEffectiveDate": "2024-10-01",
  "modules": [
    {
      "type": "DataSourcing",
      "resultName": "overdraft_events",
      "schema": "datalake",
      "table": "overdraft_events",
      "columns": ["fee_amount", "fee_waived"]
    },
    {
      "type": "Transformation",
      "resultName": "fee_summary",
      "sql": "<REMEDIATED SQL -- Remove dead ROW_NUMBER CTE entirely. Direct GROUP BY fee_waived with SUM, COUNT, AVG, ROUND. Output columns MUST be: fee_waived, total_fees, event_count, avg_fee, ifw_effective_date. ORDER BY fee_waived. Keep ROUND(,2) for total_fees and avg_fee to match V1.>"
    },
    {
      "type": "CsvFileWriter",
      "source": "fee_summary",
      "includeHeader": true,
      "writeMode": "Overwrite",
      "lineEnding": "LF",
      "outputDirectory": "{ETL_RE_OUTPUT}",
      "jobDirName": "overdraft_fee_summary",
      "fileName": "overdraft_fee_summary.csv",
      "outputTableDirName": "overdraft_fee_summary"
    }
  ]
}
```

CRITICAL: `lineEnding` is `LF` (not CRLF). `outputDirectory` is `{ETL_RE_OUTPUT}`. DataSourcing trimmed to only `fee_amount` and `fee_waived` (AP4 remediation). No `trailerFormat` (no trailer in V1).

**Steps 9-13: Register, execute, verify**
- Register: `OverdraftFeeSummary_RE` in control.jobs
- Queue 92 dates, verify Succeeded, queue Proofmark, verify 92/92 PASS
- On failure: triage/RCA/fix/retry (max 5 per PROC-02)

Proofmark queue SQL:
```sql
INSERT INTO control.proofmark_test_queue (config_path, lhs_path, rhs_path, job_key, date_key)
SELECT
  '{ETL_RE_ROOT}/EtlReverseEngineering/proofmark-configs/OverdraftFeeSummary.yaml',
  '{ETL_ROOT}/Output/curated/overdraft_fee_summary/overdraft_fee_summary/' || to_char(d.dt, 'YYYY-MM-DD') || '/overdraft_fee_summary.csv',
  '{ETL_RE_OUTPUT}/overdraft_fee_summary/overdraft_fee_summary/' || to_char(d.dt, 'YYYY-MM-DD') || '/overdraft_fee_summary.csv',
  'OverdraftFeeSummary',
  d.dt::date
FROM generate_series('2024-10-01'::date, '2024-12-31'::date, '1 day') d(dt);
```
  </action>
  <verify>
    <automated>PGPASSWORD=claude psql -h 172.18.0.1 -U claude -d atc -t -c "SELECT CASE WHEN COUNT(*) = 92 AND COUNT(*) FILTER (WHERE result = 'PASS') = 92 THEN 'OK: 92/92 PASS' ELSE 'FAIL: ' || COUNT(*) FILTER (WHERE result = 'PASS') || '/92 PASS' END FROM control.proofmark_test_queue WHERE job_key = 'OverdraftFeeSummary';"</automated>
  </verify>
  <done>OverdraftFeeSummary_RE produces 92/92 Proofmark PASS. Complete doc set exists. AP4 remediated (4 unused columns removed from DataSourcing). AP8 remediated (dead ROW_NUMBER removed).</done>
</task>

<task type="auto">
  <name>Task 2: Phase completion verification + lessons learned</name>
  <files></files>
  <action>
**Verify COMP-01 (all 3 Tier 1 jobs complete):**
Run the full suite query:
```sql
SELECT job_key, result, COUNT(*)
FROM control.proofmark_test_queue
GROUP BY job_key, result
ORDER BY job_key, result;
```
Expected: 3 rows, each showing 92 PASS (BranchDirectory, ComplianceResolutionTime, OverdraftFeeSummary).

**Verify all doc artifacts exist:**
```bash
for job in BranchDirectory ComplianceResolutionTime OverdraftFeeSummary; do
  echo "=== $job ==="
  ls jobs/$job/BRD.md jobs/$job/FSD.md jobs/$job/test-strategy.md jobs/$job/output-manifesto.md jobs/$job/anti-pattern-assessment.md
done
```

**Verify all job confs use correct output directory:**
```bash
grep -l "ETL_RE_OUTPUT" job-confs/*_re.json
```

**Capture lessons learned in the plan SUMMARY** (PROC-03). Include:
1. Template patterns that worked well and any refinements made across 3 jobs
2. Anti-pattern remediation approach: what's safe to change (DataSourcing columns, intermediate SQL), what's not (final SELECT list, output format)
3. Common pitfalls encountered and solutions
4. ETL Framework lazy reload status (confirmed working or workaround documented)
5. Proofmark patterns: config setup, queue SQL templates, verification queries
6. Time spent per job and where bottlenecks were
7. Recommendations for Tier 2 (10 multi-source jobs): what to carry forward, what to change

This is the PROC-03 deliverable -- iterative learning captured for Tier 2.
  </action>
  <verify>
    <automated>PGPASSWORD=claude psql -h 172.18.0.1 -U claude -d atc -t -c "SELECT CASE WHEN COUNT(*) = 276 AND COUNT(*) FILTER (WHERE result = 'PASS') = 276 THEN 'OK: 276/276 PASS (3 jobs x 92 dates)' ELSE 'FAIL: ' || COUNT(*) FILTER (WHERE result = 'PASS') || '/276 PASS' END FROM control.proofmark_test_queue WHERE job_key IN ('BranchDirectory', 'ComplianceResolutionTime', 'OverdraftFeeSummary');"</automated>
  </verify>
  <done>All 3 Tier 1 jobs show 92/92 PASS (276 total). All doc artifacts exist for all 3 jobs. Lessons learned captured in SUMMARY for Tier 2 consumption. COMP-01 satisfied.</done>
</task>

</tasks>

<verification>
1. OverdraftFeeSummary 92/92 PASS
2. Full suite: all 3 jobs x 92 dates = 276 PASS
3. All doc artifacts for all 3 jobs: `find jobs/ -name "*.md" | sort`
4. All job confs use {ETL_RE_OUTPUT}: `grep ETL_RE_OUTPUT job-confs/*_re.json`
5. All 3 Proofmark configs exist: `ls proofmark-configs/*.yaml`
</verification>

<success_criteria>
- OverdraftFeeSummary_RE: 92/92 Proofmark PASS
- All 3 Tier 1 jobs confirmed complete: 276/276 PASS total (COMP-01)
- Complete doc sets for all 3 jobs with traceability and evidence
- Anti-patterns assessed and remediated across all 3 jobs (ANTI-01, ANTI-02, ANTI-03)
- Lessons learned captured for Tier 2 (PROC-03)
- Zero human input across entire phase (PROC-01)
- Failure retry loop exercised or confirmed unnecessary (PROC-02)
</success_criteria>

<output>
After completion, create `.planning/phases/01-tier-1-pipeline-validation/01-03-SUMMARY.md`

This is the CRITICAL summary -- it must contain:
1. Tier 1 completion status (276/276 PASS)
2. Lessons learned for Tier 2 (PROC-03 deliverable):
   - Template patterns that scale
   - Anti-pattern remediation rules of thumb
   - Common failure modes and fixes
   - Infrastructure notes (lazy reload, Proofmark behavior)
   - Recommendations for multi-source join jobs
3. Any issues that need addressing before Tier 2
</output>
