---
phase: 02-review-branching-and-counter-mechanics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/workflow_engine/transitions.py
  - src/workflow_engine/nodes.py
  - tests/test_transitions.py
  - tests/test_nodes.py
autonomous: true
requirements: [RB-01, RB-02, RB-03, RB-05]

must_haves:
  truths:
    - "TRANSITION_TABLE contains CONDITIONAL and FAIL edges for all 6 review nodes"
    - "7 response nodes exist as StubWorkNode instances in the registry"
    - "Response node SUCCESS routes back to the same reviewer"
    - "FAIL at a review node maps to the correct original write node (rewind target)"
    - "Approve still routes to next happy-path node (no regression)"
  artifacts:
    - path: "src/workflow_engine/transitions.py"
      provides: "REVIEW_ROUTING dict, expanded TRANSITION_TABLE with Conditional/Fail/response-SUCCESS edges"
      contains: "REVIEW_ROUTING"
    - path: "src/workflow_engine/nodes.py"
      provides: "7 response node stubs in _RESPONSE_NODE_DESCRIPTIONS, registered in create_node_registry"
      contains: "_RESPONSE_NODE_DESCRIPTIONS"
    - path: "tests/test_transitions.py"
      provides: "TestReviewRouting class validating all new edges"
      contains: "TestReviewRouting"
    - path: "tests/test_nodes.py"
      provides: "test_response_nodes_exist verifying 7 response nodes in registry"
      contains: "test_response_nodes_exist"
  key_links:
    - from: "src/workflow_engine/transitions.py"
      to: "src/workflow_engine/nodes.py"
      via: "response node names must match between REVIEW_ROUTING and _RESPONSE_NODE_DESCRIPTIONS"
      pattern: "REVIEW_ROUTING.*Response"
    - from: "src/workflow_engine/transitions.py"
      to: "TRANSITION_TABLE"
      via: "loop over REVIEW_ROUTING adds edges to TRANSITION_TABLE"
      pattern: "TRANSITION_TABLE\\[\\(review_node"
---

<objective>
Expand the transition table with review branching edges and add 7 response node stubs.

Purpose: The transition table must contain all routing data (Conditional -> response, Fail -> rewind target, response SUCCESS -> reviewer) before Plan 02 can implement the engine logic that uses these edges.
Output: Extended TRANSITION_TABLE, REVIEW_ROUTING dict, 7 response node stubs, comprehensive tests for the data layer.
</objective>

<execution_context>
@/home/sandbox/.claude/get-shit-done/workflows/execute-plan.md
@/home/sandbox/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-review-branching-and-counter-mechanics/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/workflow_engine/models.py:
```python
class Outcome(Enum):
    SUCCESS = auto()
    FAILURE = auto()
    APPROVE = auto()
    CONDITIONAL = auto()
    FAIL = auto()

class NodeType(Enum):
    WORK = auto()
    REVIEW = auto()
```

From src/workflow_engine/transitions.py:
```python
HAPPY_PATH: list[str] = [...]  # 27 nodes in order
_REVIEW_NODES: set[str] = {...}  # 13 review nodes
NODE_TYPES: dict[str, NodeType] = {...}  # all 27 nodes classified
TRANSITION_TABLE: dict[tuple[str, Outcome], str] = {}  # happy-path edges only currently
```

From src/workflow_engine/nodes.py:
```python
class StubWorkNode(Node):
    def __init__(self, node_name: str, description: str, *, rng: random.Random | None = None) -> None: ...

def create_node_registry(rng: random.Random | None = None) -> dict[str, Node]:
    # Currently only registers HAPPY_PATH nodes
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add REVIEW_ROUTING and expand TRANSITION_TABLE with branching edges</name>
  <files>src/workflow_engine/transitions.py, tests/test_transitions.py</files>
  <behavior>
    - Test: REVIEW_ROUTING has exactly 6 entries (ReviewBrd, ReviewBdd, ReviewFsd, ReviewJobArtifacts, ReviewProofmarkConfig, ReviewUnitTests)
    - Test: Each review node has (node, CONDITIONAL) -> response_node in TRANSITION_TABLE
    - Test: Each review node has (node, FAIL) -> rewind_target in TRANSITION_TABLE
    - Test: Each response node has (response_node, SUCCESS) -> review_node in TRANSITION_TABLE
    - Test: Approve edges still work (no regression) -- (ReviewBrd, APPROVE) -> WriteBddTestArch, etc.
    - Test: Rewind targets are the ORIGINAL write nodes: WriteBrd, WriteBddTestArch, WriteFsd, BuildJobArtifacts, BuildProofmarkConfig, BuildUnitTests
    - Test: validate_transition_table() still returns empty (valid)
  </behavior>
  <action>
1. In tests/test_transitions.py, add a `TestReviewRouting` class with tests covering all the behaviors above. Tests should import REVIEW_ROUTING from transitions and validate all edges. Write tests FIRST -- they will fail (RED).

2. In src/workflow_engine/transitions.py, add:
   - `REVIEW_ROUTING: dict[str, tuple[str, str]]` mapping each of the 6 review nodes to (response_node, rewind_target). Use the exact mapping from the research doc:
     - ReviewBrd -> (WriteBrdResponse, WriteBrd)
     - ReviewBdd -> (WriteBddResponse, WriteBddTestArch)
     - ReviewFsd -> (WriteFsdResponse, WriteFsd)
     - ReviewJobArtifacts -> (BuildJobArtifactsResponse, BuildJobArtifacts)
     - ReviewProofmarkConfig -> (BuildProofmarkResponse, BuildProofmarkConfig)
     - ReviewUnitTests -> (BuildUnitTestsResponse, BuildUnitTests)
   - Loop over REVIEW_ROUTING to add 3 edges per review node to TRANSITION_TABLE:
     - (review_node, Outcome.CONDITIONAL) -> response_node
     - (review_node, Outcome.FAIL) -> rewind_target
     - (response_node, Outcome.SUCCESS) -> review_node
   - Also add NODE_TYPES entries for all 7 response nodes as NodeType.WORK (response nodes are work nodes per research Pitfall 5).
   - Note: TriageProofmarkFailures is a response node per RB-05 but has no review routing in Phase 2. Add it to NODE_TYPES as WORK but do NOT add transition table entries for it yet (Phase 3 scope). Optionally add a placeholder `(TriageProofmarkFailures, SUCCESS) -> ExecuteProofmark` per research recommendation.
   - Do NOT modify validate_transition_table() -- it only validates happy-path edges, and that's correct behavior.

3. Run tests -- all should pass (GREEN).

IMPORTANT: FinalSignOff Conditional/Fail handling is NOT Phase 2 scope (per research Open Question 2). Do not add branching edges for FinalSignOff or FBR_* nodes -- those are Phase 3.
  </action>
  <verify>
    <automated>cd /workspace/EtlReverseEngineering && python -m pytest tests/test_transitions.py -v -x</automated>
  </verify>
  <done>TRANSITION_TABLE has 27 happy-path edges + 18 review branching edges (6 CONDITIONAL + 6 FAIL + 6 response SUCCESS). REVIEW_ROUTING is exported. All existing tests still pass. New TestReviewRouting tests all green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 7 response node stubs and register them</name>
  <files>src/workflow_engine/nodes.py, tests/test_nodes.py</files>
  <behavior>
    - Test: create_node_registry() returns entries for all 7 response nodes (WriteBrdResponse, WriteBddResponse, WriteFsdResponse, BuildJobArtifactsResponse, BuildProofmarkResponse, BuildUnitTestsResponse, TriageProofmarkFailures)
    - Test: All 7 response nodes are StubWorkNode instances (not StubReviewNode)
    - Test: All 7 response nodes have non-empty descriptions
    - Test: Response nodes in deterministic mode (rng=None) return SUCCESS
    - Test: Response nodes in RNG mode return SUCCESS or FAILURE (not APPROVE/CONDITIONAL/FAIL)
  </behavior>
  <action>
1. In tests/test_nodes.py, add `test_response_nodes_exist` and `test_response_nodes_are_work_type` tests. Write them FIRST (RED).

2. In src/workflow_engine/nodes.py:
   - Add `_RESPONSE_NODE_DESCRIPTIONS: dict[str, str]` with descriptions for all 7 response nodes (use descriptions from research doc).
   - Modify `create_node_registry()` to also register response nodes as StubWorkNode instances. The function currently iterates only HAPPY_PATH. Add a second loop over `_RESPONSE_NODE_DESCRIPTIONS` to create StubWorkNode entries for each response node.
   - Response nodes use the SAME rng parameter as happy-path nodes.

3. Run tests (GREEN).

IMPORTANT: Response nodes are WORK type. They return SUCCESS/FAILURE, never APPROVE/CONDITIONAL/FAIL. This is critical for Phase 2 engine logic.
  </action>
  <verify>
    <automated>cd /workspace/EtlReverseEngineering && python -m pytest tests/test_nodes.py -v -x</automated>
  </verify>
  <done>create_node_registry() returns 34 entries (27 happy-path + 7 response nodes). All response nodes are StubWorkNode. test_response_nodes_exist is green. Full test suite green.</done>
</task>

</tasks>

<verification>
cd /workspace/EtlReverseEngineering && python -m pytest tests/ -v
All existing Phase 1 tests pass (no regression). New tests for review routing and response nodes pass.
</verification>

<success_criteria>
- TRANSITION_TABLE has all review branching edges (CONDITIONAL, FAIL, response SUCCESS)
- REVIEW_ROUTING dict exported from transitions.py
- 7 response nodes in registry as StubWorkNode
- NODE_TYPES includes response nodes as WORK
- All tests green including new TestReviewRouting and test_response_nodes_exist
</success_criteria>

<output>
After completion, create `.planning/phases/02-review-branching-and-counter-mechanics/02-01-SUMMARY.md`
</output>
