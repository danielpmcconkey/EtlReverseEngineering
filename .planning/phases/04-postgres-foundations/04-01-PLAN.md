---
phase: 04-postgres-foundations
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/workflow_engine/db.py
  - src/workflow_engine/schema.sql
  - tests/test_db.py
  - tests/conftest.py
  - pyproject.toml
autonomous: true
requirements: [TQ-01, JS-01, JS-02]

must_haves:
  truths:
    - "re_task_queue table exists in control schema with FIFO ordering on created_at"
    - "re_job_state table exists in control schema with all JobState fields"
    - "Job state can be written to and read from Postgres by any caller"
    - "A task can be enqueued and retrieved in FIFO order"
  artifacts:
    - path: "src/workflow_engine/schema.sql"
      provides: "DDL for re_task_queue and re_job_state tables"
      contains: "CREATE TABLE control.re_task_queue"
    - path: "src/workflow_engine/db.py"
      provides: "Connection pool, task queue CRUD, job state CRUD"
      exports: ["get_pool", "enqueue_task", "save_job_state", "load_job_state"]
    - path: "tests/test_db.py"
      provides: "Tests for all DB operations"
      min_lines: 80
  key_links:
    - from: "src/workflow_engine/db.py"
      to: "control.re_task_queue"
      via: "psycopg connection pool"
      pattern: "re_task_queue"
    - from: "src/workflow_engine/db.py"
      to: "control.re_job_state"
      via: "psycopg connection pool"
      pattern: "re_job_state"
---

<objective>
Create the Postgres schema and Python DB layer for the task queue and job state tables.

Purpose: Phase 4 needs Postgres-backed storage that replaces in-memory JobState and provides a claimable task queue. This plan builds the DDL, connection pool, and all CRUD operations with tests.

Output: `schema.sql` DDL file, `db.py` module with psycopg3 connection pool and CRUD functions, `test_db.py` with full coverage.
</objective>

<execution_context>
@/workspace/EtlReverseEngineering/.planning/ROADMAP.md
@/workspace/EtlReverseEngineering/.planning/REQUIREMENTS.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Postgres connection: host=172.18.0.1, port=5432, user=claude, password=claude, dbname=atc
The `control` schema already exists. There are existing tables (task_queue, jobs, etc.) from a prior project -- do NOT touch them. Our tables use the `re_` prefix.

Existing in-memory JobState (src/workflow_engine/models.py):
```python
@dataclass
class JobState:
    job_id: str
    current_node: str = "LocateOgSourceFiles"
    status: str = "RUNNING"
    main_retry_count: int = 0
    conditional_counts: dict[str, int] = field(default_factory=dict)
    last_rejection_reason: str | None = None
    fbr_return_pending: bool = False
    triage_results: dict[str, str] = field(default_factory=dict)
    triage_rewind_target: str | None = None
```

This plan does NOT modify the existing JobState dataclass or engine.py. It creates parallel Postgres storage that later phases will wire in.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema DDL and psycopg3 dependency</name>
  <files>src/workflow_engine/schema.sql, pyproject.toml</files>
  <behavior>
    - schema.sql can be applied to the database without errors
    - re_task_queue has: id (bigserial PK), job_id (text NOT NULL), node_name (text NOT NULL), status (text NOT NULL DEFAULT 'pending'), created_at (timestamptz NOT NULL DEFAULT now()), claimed_at (timestamptz), completed_at (timestamptz)
    - re_task_queue has index on (status, created_at) for FIFO claiming
    - re_task_queue has unique partial index: only one row per job_id WHERE status IN ('pending', 'claimed') -- enforces JS-03
    - re_job_state has: job_id (text PK), current_node (text NOT NULL), status (text NOT NULL), main_retry_count (int NOT NULL DEFAULT 0), conditional_counts (jsonb NOT NULL DEFAULT '{}'), last_rejection_reason (text), fbr_return_pending (boolean NOT NULL DEFAULT false), triage_results (jsonb NOT NULL DEFAULT '{}'), triage_rewind_target (text), updated_at (timestamptz NOT NULL DEFAULT now())
    - Both tables use IF NOT EXISTS
    - psycopg[binary] added to pyproject.toml dependencies
  </behavior>
  <action>
    1. Add `psycopg[binary]>=3.1` to pyproject.toml dependencies list (alongside structlog).
    2. Run `pip install -e ".[dev]"` to install psycopg.
    3. Create `src/workflow_engine/schema.sql` with:
       - `CREATE TABLE IF NOT EXISTS control.re_task_queue` with columns: id (bigserial PK), job_id (text NOT NULL), node_name (text NOT NULL), status (text NOT NULL DEFAULT 'pending'), created_at (timestamptz NOT NULL DEFAULT now()), claimed_at (timestamptz NULL), completed_at (timestamptz NULL).
       - Index: `CREATE INDEX IF NOT EXISTS ix_re_task_queue_fifo ON control.re_task_queue (created_at) WHERE status = 'pending'` -- supports FIFO claiming.
       - Unique partial index: `CREATE UNIQUE INDEX IF NOT EXISTS ix_re_task_queue_one_active ON control.re_task_queue (job_id) WHERE status IN ('pending', 'claimed')` -- enforces at-most-one-active-per-job (JS-03).
       - `CREATE TABLE IF NOT EXISTS control.re_job_state` with all JobState fields mapped: job_id (text PK), current_node (text NOT NULL DEFAULT 'LocateOgSourceFiles'), status (text NOT NULL DEFAULT 'RUNNING'), main_retry_count (integer NOT NULL DEFAULT 0), conditional_counts (jsonb NOT NULL DEFAULT '{}'), last_rejection_reason (text), fbr_return_pending (boolean NOT NULL DEFAULT false), triage_results (jsonb NOT NULL DEFAULT '{}'), triage_rewind_target (text), updated_at (timestamptz NOT NULL DEFAULT now()).
    4. Apply schema: `psql -h 172.18.0.1 -U claude -d atc -f src/workflow_engine/schema.sql`.
  </action>
  <verify>
    <automated>PGPASSWORD=claude psql -h 172.18.0.1 -U claude -d atc -c "\d control.re_task_queue" && PGPASSWORD=claude psql -h 172.18.0.1 -U claude -d atc -c "\d control.re_job_state"</automated>
  </verify>
  <done>Both tables exist in control schema with correct columns, indexes, and constraints. psycopg installed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DB module with connection pool and CRUD operations</name>
  <files>src/workflow_engine/db.py, tests/test_db.py, tests/conftest.py</files>
  <behavior>
    - Test: get_pool() returns a psycopg ConnectionPool connected to atc database
    - Test: enqueue_task(job_id, node_name) inserts a pending row and returns the task id
    - Test: enqueue_task for same job_id while one is active raises IntegrityError (unique partial index)
    - Test: claim_task() returns the oldest pending task and sets status='claimed', claimed_at=now()
    - Test: claim_task() returns None when no pending tasks exist
    - Test: complete_task(task_id) sets status='completed', completed_at=now()
    - Test: fail_task(task_id, error_msg) sets status='failed', completed_at=now(), stores error
    - Test: save_job_state(job_state) upserts all fields to re_job_state (INSERT ON CONFLICT UPDATE)
    - Test: load_job_state(job_id) returns a JobState dataclass populated from Postgres
    - Test: load_job_state for nonexistent job_id returns None
    - Test: save then load round-trips all fields including conditional_counts dict and triage_results dict
  </behavior>
  <action>
    1. Create `src/workflow_engine/db.py` with:
       - Module-level `_pool: ConnectionPool | None = None`
       - `get_pool() -> ConnectionPool` -- lazy-initializes a psycopg.pool.ConnectionPool with conninfo from env var `RE_DATABASE_URL` defaulting to `host=172.18.0.1 port=5432 dbname=atc user=claude password=claude`. Min size 1, max size 10.
       - `close_pool()` -- closes the pool (for test cleanup).
       - `ensure_schema()` -- reads schema.sql from same package dir and executes it. Called once at startup.
       - `enqueue_task(job_id: str, node_name: str) -> int` -- INSERT INTO control.re_task_queue (job_id, node_name) VALUES (%s, %s) RETURNING id. Uses pool connection context manager.
       - `claim_task() -> dict | None` -- `SELECT id, job_id, node_name FROM control.re_task_queue WHERE status = 'pending' ORDER BY created_at FOR UPDATE SKIP LOCKED LIMIT 1`. If row found, UPDATE status='claimed', claimed_at=now(), return dict. If no row, return None. Single transaction.
       - `complete_task(task_id: int) -> None` -- UPDATE status='completed', completed_at=now() WHERE id=task_id.
       - `fail_task(task_id: int, error: str) -> None` -- UPDATE status='failed', completed_at=now(), and store error somehow (add an error_message column to schema if not present -- actually, keep it simple: log the error but don't add a column. The queue is about flow control, not error storage. On second thought, DO NOT add error_message -- keep the table lean. Just set status='failed' and completed_at.)
       - Actually, let's keep fail_task simple: `fail_task(task_id: int) -> None` -- just UPDATE status='failed', completed_at=now().
       - `save_job_state(state: JobState) -> None` -- INSERT INTO control.re_job_state ... ON CONFLICT (job_id) DO UPDATE SET all fields. Use json.dumps for conditional_counts and triage_results.
       - `load_job_state(job_id: str) -> JobState | None` -- SELECT * FROM control.re_job_state WHERE job_id = %s. Map back to JobState dataclass. Use json.loads for jsonb fields. Return None if not found.

    2. Update `tests/conftest.py` to add a `db_cleanup` fixture (autouse for test_db.py or session-scoped) that truncates both re_ tables before each test and closes pool after all tests.

    3. Create `tests/test_db.py` with tests for every behavior listed above. Each test uses the real Postgres (integration tests -- this is infrastructure code, mocking would be pointless). Tests should be independent (truncate tables in fixture).

    Important: Use `psycopg.pool.ConnectionPool` (psycopg 3), NOT psycopg2. Import as `import psycopg` and `from psycopg.rows import dict_row`.
  </action>
  <verify>
    <automated>cd /workspace/EtlReverseEngineering && python -m pytest tests/test_db.py -v</automated>
  </verify>
  <done>All CRUD operations work against real Postgres. Round-trip of JobState preserves all fields including nested dicts. enqueue/claim/complete lifecycle works. Pool connects and cleans up.</done>
</task>

</tasks>

<verification>
1. Both tables exist: `\d control.re_task_queue` and `\d control.re_job_state` show correct schemas
2. All tests pass: `python -m pytest tests/test_db.py -v`
3. No existing tests broken: `python -m pytest tests/ -v`
</verification>

<success_criteria>
- re_task_queue and re_job_state tables exist in control schema
- Python CRUD operations (enqueue, claim, complete, save_state, load_state) all work
- FIFO ordering confirmed by test
- Unique partial index enforces one-active-per-job at DB level
- All tests pass including existing v0.1 tests
</success_criteria>

<output>
After completion, create `.planning/phases/04-postgres-foundations/04-01-SUMMARY.md`
</output>
