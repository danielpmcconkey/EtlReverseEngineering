---
phase: 04-postgres-foundations
plan: 02
type: tdd
wave: 2
depends_on: ["04-01"]
files_modified:
  - tests/test_db_concurrency.py
  - src/workflow_engine/db.py
autonomous: true
requirements: [TQ-02, JS-03]

must_haves:
  truths:
    - "Two concurrent workers claiming from the queue never get the same task"
    - "At most one active (pending or claimed) task per job exists on the queue"
    - "SKIP LOCKED causes second claimer to skip the locked row, not block"
  artifacts:
    - path: "tests/test_db_concurrency.py"
      provides: "Concurrency tests proving SKIP LOCKED and one-active-per-job"
      min_lines: 60
  key_links:
    - from: "tests/test_db_concurrency.py"
      to: "src/workflow_engine/db.py"
      via: "imports claim_task, enqueue_task"
      pattern: "from workflow_engine.db import"
---

<objective>
Prove that the task queue handles concurrency correctly: SKIP LOCKED prevents double-claiming, and the unique partial index enforces one-active-task-per-job.

Purpose: TQ-02 and JS-03 are the concurrency guarantees that make parallel workers safe. The schema from Plan 01 has the constraints -- this plan proves they work under concurrent access.

Output: `test_db_concurrency.py` with multi-connection tests that exercise real Postgres locking behavior.
</objective>

<execution_context>
@/workspace/EtlReverseEngineering/.planning/ROADMAP.md
@/workspace/EtlReverseEngineering/.planning/REQUIREMENTS.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-postgres-foundations/04-01-PLAN.md

Depends on Plan 01 having created:
- `src/workflow_engine/db.py` with `get_pool()`, `enqueue_task()`, `claim_task()`, `complete_task()`
- `control.re_task_queue` with unique partial index `ix_re_task_queue_one_active`

<interfaces>
From src/workflow_engine/db.py (created by Plan 01):
```python
def get_pool() -> ConnectionPool: ...
def enqueue_task(job_id: str, node_name: str) -> int: ...
def claim_task() -> dict | None: ...  # Returns {"id": int, "job_id": str, "node_name": str} or None
def complete_task(task_id: int) -> None: ...
def fail_task(task_id: int) -> None: ...
def save_job_state(state: JobState) -> None: ...
def load_job_state(job_id: str) -> JobState | None: ...
def close_pool() -> None: ...
def ensure_schema() -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SKIP LOCKED concurrency tests</name>
  <files>tests/test_db_concurrency.py</files>
  <behavior>
    - Test: Two connections both try to claim the same single pending task. One gets it, the other gets None. Neither blocks.
    - Test: Three tasks enqueued, two concurrent claimers each get a different task (no overlap in returned task IDs).
    - Test: After a claimed task is completed, a new task for the same job_id CAN be enqueued (the unique partial index only blocks pending+claimed).
    - Test: Enqueueing a second task for a job_id that already has a pending task raises psycopg.errors.UniqueViolation.
    - Test: Enqueueing a second task for a job_id that already has a claimed task raises psycopg.errors.UniqueViolation.
  </behavior>
  <action>
    These tests need to use raw psycopg connections (not the pool) to control transaction boundaries manually. The SKIP LOCKED tests require two connections with overlapping transactions:

    1. Create `tests/test_db_concurrency.py`.

    2. For SKIP LOCKED double-claim test:
       - Enqueue one task for "job-A".
       - Open two separate psycopg connections (direct, not pooled).
       - Connection 1: BEGIN, SELECT ... FOR UPDATE SKIP LOCKED LIMIT 1 -- gets the row.
       - Connection 2: BEGIN, SELECT ... FOR UPDATE SKIP LOCKED LIMIT 1 -- gets None (row is locked, SKIP LOCKED skips it).
       - Assert conn1 got a row, conn2 got None.
       - ROLLBACK both.

    3. For two-claimers-get-different-tasks test:
       - Enqueue 3 tasks (different job_ids).
       - Conn1 claims (gets task, holds transaction open).
       - Conn2 claims (gets DIFFERENT task, skipping conn1's locked row).
       - Assert the two task IDs are different.
       - ROLLBACK both.

    4. For one-active-per-job tests:
       - Enqueue task for "job-X". Try enqueue again for "job-X" -- expect UniqueViolation.
       - Enqueue and claim task for "job-Y". Try enqueue for "job-Y" -- expect UniqueViolation (still active -- claimed counts).
       - Enqueue, claim, complete task for "job-Z". Enqueue again for "job-Z" -- should succeed (completed rows don't block).

    5. Use a fixture that truncates re_task_queue before each test and ensures connections are closed after.

    Important: Do NOT use `claim_task()` from db.py for the SKIP LOCKED tests -- use raw SQL so we control when transactions commit. The point is proving Postgres behavior. For the one-active-per-job tests, using db.py functions is fine since those are single-statement operations.
  </action>
  <verify>
    <automated>cd /workspace/EtlReverseEngineering && python -m pytest tests/test_db_concurrency.py -v</automated>
  </verify>
  <done>All concurrency tests pass. SKIP LOCKED prevents double-claiming. Unique partial index prevents multiple active tasks per job. Completed tasks don't block new enqueues for the same job.</done>
</task>

</tasks>

<verification>
1. Concurrency tests pass: `python -m pytest tests/test_db_concurrency.py -v`
2. All tests still pass: `python -m pytest tests/ -v`
3. Manual sanity: the SKIP LOCKED test actually uses two connections with overlapping transactions (not sequential -- that would prove nothing)
</verification>

<success_criteria>
- Double-claim prevention proven with real concurrent Postgres connections
- One-active-per-job constraint proven at DB level (not application logic)
- All existing tests unbroken
</success_criteria>

<output>
After completion, create `.planning/phases/04-postgres-foundations/04-02-SUMMARY.md`
</output>
