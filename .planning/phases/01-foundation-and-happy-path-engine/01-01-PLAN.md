---
phase: 01-foundation-and-happy-path-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/workflow_engine/__init__.py
  - src/workflow_engine/models.py
  - src/workflow_engine/transitions.py
  - src/workflow_engine/nodes.py
  - src/workflow_engine/logging.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_models.py
  - tests/test_transitions.py
  - tests/test_nodes.py
  - tests/test_logging.py
autonomous: true
requirements:
  - SM-01
  - SM-02
  - SM-03
  - HP-02
  - HP-03
  - HP-04
  - LOG-01
  - PS-01
  - PS-02

must_haves:
  truths:
    - "JobState tracks current_node, main_retry_count, and conditional_counts dict"
    - "EngineConfig exposes max_main_retries (default 5) and max_conditional_per_node (default 3)"
    - "Transition table is a dict keyed by (node_name, Outcome) tuples covering all 27 happy-path edges"
    - "NODE_TYPES maps every node to WORK or REVIEW"
    - "StubWorkNode returns SUCCESS or FAILURE, StubReviewNode returns APPROVE, CONDITIONAL, or FAIL"
    - "Every stub has a docstring describing what the real agent will do"
    - "structlog is configured for JSON output with timestamps and log levels"
    - "Source is importable as workflow_engine from src/"
  artifacts:
    - path: "src/workflow_engine/models.py"
      provides: "JobState, Outcome, NodeType, EngineConfig dataclasses/enums"
      exports: ["JobState", "Outcome", "NodeType", "EngineConfig"]
    - path: "src/workflow_engine/transitions.py"
      provides: "TRANSITION_TABLE dict, HAPPY_PATH list, NODE_TYPES dict"
      exports: ["TRANSITION_TABLE", "HAPPY_PATH", "NODE_TYPES"]
    - path: "src/workflow_engine/nodes.py"
      provides: "Node ABC, StubWorkNode, StubReviewNode"
      exports: ["Node", "StubWorkNode", "StubReviewNode"]
    - path: "src/workflow_engine/logging.py"
      provides: "structlog JSON configuration"
      exports: ["configure_logging"]
    - path: "tests/test_models.py"
      provides: "Unit tests for SM-01, SM-03, ENG-03"
    - path: "tests/test_transitions.py"
      provides: "Unit tests for SM-02"
    - path: "tests/test_nodes.py"
      provides: "Unit tests for HP-02, HP-03, HP-04"
    - path: "tests/test_logging.py"
      provides: "Unit tests for LOG-01"
  key_links:
    - from: "src/workflow_engine/transitions.py"
      to: "src/workflow_engine/models.py"
      via: "imports Outcome, NodeType"
      pattern: "from workflow_engine\\.models import"
    - from: "src/workflow_engine/nodes.py"
      to: "src/workflow_engine/models.py"
      via: "imports Outcome, JobState"
      pattern: "from workflow_engine\\.models import"
    - from: "src/workflow_engine/transitions.py"
      to: "state-machine-transitions.md"
      via: "HAPPY_PATH list matches all 27 nodes in order"
      pattern: "HAPPY_PATH.*list\\[str\\]"
---

<objective>
Build the data layer and test scaffolding for the workflow engine: state model, transition table, node stubs, and logging config.

Purpose: Everything the engine (Plan 02) needs to exist before it can wire things together. This plan creates all contracts, types, and data structures.
Output: Importable workflow_engine package with models, transitions, nodes, logging -- all with passing tests.
</objective>

<execution_context>
@/home/sandbox/.claude/get-shit-done/workflows/execute-plan.md
@/home/sandbox/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-happy-path-engine/01-RESEARCH.md
@.planning/phases/01-foundation-and-happy-path-engine/01-VALIDATION.md

Source of truth for the 27 happy-path nodes, node types, stages, and blueprints:
@/workspace/AtcStrategy/POC6/BDsNotes/state-machine-transitions.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project scaffolding, models, and transition table</name>
  <files>
    pyproject.toml
    src/workflow_engine/__init__.py
    src/workflow_engine/models.py
    src/workflow_engine/transitions.py
    tests/__init__.py
    tests/conftest.py
    tests/test_models.py
    tests/test_transitions.py
  </files>
  <behavior>
    - test_job_state_fields: JobState has job_id (str), current_node (str, default "LocateOgSourceFiles"), status (str, default "RUNNING"), main_retry_count (int, default 0), conditional_counts (dict, default empty), last_rejection_reason (str|None, default None)
    - test_job_state_is_dataclass: JobState is a dataclass (dataclasses.is_dataclass returns True)
    - test_engine_config_defaults: EngineConfig has n_jobs (default 5), max_main_retries (default 5), max_conditional_per_node (default 3)
    - test_outcome_enum: Outcome has exactly 5 members: SUCCESS, FAILURE, APPROVE, CONDITIONAL, FAIL
    - test_node_type_enum: NodeType has exactly 2 members: WORK, REVIEW
    - test_table_is_dict: TRANSITION_TABLE is a dict, not a function or class
    - test_happy_path_has_27_nodes: HAPPY_PATH list has exactly 27 entries
    - test_happy_path_order: First node is "LocateOgSourceFiles", last is "FinalSignOff"
    - test_every_happy_path_node_has_outbound_edge: Every node in HAPPY_PATH has at least one entry in TRANSITION_TABLE
    - test_node_types_cover_all_nodes: NODE_TYPES has an entry for every node in HAPPY_PATH
    - test_work_nodes_use_success: All WORK nodes have (node, Outcome.SUCCESS) in TRANSITION_TABLE
    - test_review_nodes_use_approve: All REVIEW nodes have (node, Outcome.APPROVE) in TRANSITION_TABLE
    - test_no_state_bleed: Two fresh JobState instances share no mutable state (conditional_counts are independent dicts)
  </behavior>
  <action>
    1. Create pyproject.toml with:
       - [project] name="workflow-engine", version="0.1.0", requires-python=">=3.12", dependencies=["structlog>=25.0"]
       - [tool.pytest.ini_options] testpaths=["tests"], pythonpath=["src"]
       - [tool.ruff] target-version="py312"
       - [tool.mypy] python_version="3.12", strict=true

    2. Install deps: pip install -e ".[dev]" or pip install structlog pytest pytest-cov ruff mypy

    3. Create src/workflow_engine/__init__.py exporting key names.

    4. Create src/workflow_engine/models.py:
       - Outcome enum with 5 members: SUCCESS, FAILURE, APPROVE, CONDITIONAL, FAIL (use auto())
       - NodeType enum: WORK, REVIEW
       - JobState dataclass: job_id (str), current_node (str, default "LocateOgSourceFiles"), status (str, default "RUNNING"), main_retry_count (int, default 0), conditional_counts (dict[str, int], default_factory=dict), last_rejection_reason (str | None, default None)
       - EngineConfig dataclass: n_jobs (int, default 5), max_main_retries (int, default 5), max_conditional_per_node (int, default 3), seed (int | None, default None)

    5. Create src/workflow_engine/transitions.py:
       - HAPPY_PATH: list[str] with all 27 nodes in exact order from the transition table source doc
       - NODE_TYPES: dict[str, NodeType] mapping every node. Review nodes: ReviewBrd, ReviewBdd, ReviewFsd, ReviewJobArtifacts, ReviewProofmarkConfig, ReviewUnitTests, FinalSignOff. FBR gates (FBR_BrdCheck through FBR_UnitTestCheck) are REVIEW type. All others are WORK.
       - TRANSITION_TABLE: dict[tuple[str, Outcome], str] with happy-path edges only. Work nodes keyed on SUCCESS, review nodes keyed on APPROVE. The final edge is ("FinalSignOff", Outcome.APPROVE) -> "COMPLETE".
       - validate_transition_table() function that checks every HAPPY_PATH node has the correct outbound edge for its NodeType.

    6. Create tests/conftest.py with fixtures: fresh_job_state (factory), default_config.

    7. Write tests/test_models.py and tests/test_transitions.py per the behavior spec above.

    IMPORTANT: The HAPPY_PATH list must exactly match these 27 nodes from the transition table source doc:
    LocateOgSourceFiles, InventoryOutputs, InventoryDataSources, NoteDependencies, WriteBrd, ReviewBrd, WriteBddTestArch, ReviewBdd, WriteFsd, ReviewFsd, BuildJobArtifacts, ReviewJobArtifacts, BuildProofmarkConfig, ReviewProofmarkConfig, BuildUnitTests, ReviewUnitTests, ExecuteUnitTests, Publish, FBR_BrdCheck, FBR_BddCheck, FBR_FsdCheck, FBR_ArtifactCheck, FBR_ProofmarkCheck, FBR_UnitTestCheck, ExecuteJobRuns, ExecuteProofmark, FinalSignOff
  </action>
  <verify>
    <automated>cd /workspace/EtlReverseEngineering && python -m pytest tests/test_models.py tests/test_transitions.py -x -v</automated>
  </verify>
  <done>
    - pyproject.toml exists with correct config
    - `python -c "import workflow_engine"` works
    - All model and transition tests pass
    - HAPPY_PATH has exactly 27 nodes matching the source transition table
    - TRANSITION_TABLE has 27 happy-path edges with correct Outcome types per NodeType
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Node stubs and structlog configuration</name>
  <files>
    src/workflow_engine/nodes.py
    src/workflow_engine/logging.py
    tests/test_nodes.py
    tests/test_logging.py
  </files>
  <behavior>
    - test_stubs_have_descriptions: Every stub created from HAPPY_PATH + response nodes has a non-empty docstring
    - test_work_stub_outcomes: StubWorkNode.execute() returns only SUCCESS or FAILURE (never APPROVE/CONDITIONAL/FAIL)
    - test_review_stub_outcomes: StubReviewNode.execute() returns only APPROVE, CONDITIONAL, or FAIL (never SUCCESS/FAILURE)
    - test_deterministic_mode: When seed is None, stubs return SUCCESS/APPROVE (deterministic happy path)
    - test_rng_mode: When seed is set, stubs produce varied outcomes across multiple calls
    - test_node_abc: Node is abstract -- cannot be instantiated directly
    - test_json_output: configure_logging() produces JSON-formatted output with "event", "timestamp", "level" keys
    - test_bound_context: structlog bound logger carries context (e.g., job_id) across log calls
  </behavior>
  <action>
    1. Create src/workflow_engine/nodes.py:
       - Node ABC with abstract method: execute(job: JobState) -> Outcome
       - StubWorkNode(Node): Takes node_name (str), description (str), and optional random.Random instance.
         - execute(): If no RNG, return SUCCESS. If RNG, return choice([SUCCESS, FAILURE]).
         - Docstring from description parameter.
       - StubReviewNode(Node): Same pattern but returns APPROVE when deterministic, choice([APPROVE, CONDITIONAL, FAIL]) with RNG.
       - create_node_registry(rng: random.Random | None) -> dict[str, Node]: Factory that creates a stub for every node in HAPPY_PATH, using NODE_TYPES to pick StubWorkNode vs StubReviewNode. Description strings should be one-liners referencing the blueprint name from the transition table, e.g.:
         - LocateOgSourceFiles: "og-locator: Locates original source files for the ETL job"
         - ReviewBrd: "brd-reviewer: Reviews the Business Requirements Document for correctness"
         - (Cover all 27 nodes with meaningful descriptions based on stage and blueprint from the transition table source doc)

    2. Create src/workflow_engine/logging.py:
       - configure_logging() function setting up structlog with:
         - TimeStamper(fmt="iso")
         - add_log_level processor
         - JSONRenderer()
         - PrintLoggerFactory()
         - wrapper_class=structlog.make_filtering_bound_logger(0) for DEBUG+

    3. Write tests/test_nodes.py and tests/test_logging.py per the behavior spec.
       - For logging tests: capture stdout, parse JSON, assert keys present.
       - For node tests: use a seeded Random for reproducibility.
  </action>
  <verify>
    <automated>cd /workspace/EtlReverseEngineering && python -m pytest tests/test_nodes.py tests/test_logging.py -x -v</automated>
  </verify>
  <done>
    - Node ABC cannot be instantiated
    - All 27 HAPPY_PATH nodes have stubs with descriptions
    - Deterministic mode (no seed) always returns SUCCESS/APPROVE
    - RNG mode produces varied outcomes
    - structlog outputs valid JSON with timestamp, level, and event fields
    - All node and logging tests pass
  </done>
</task>

</tasks>

<verification>
Full test suite for Plan 01:
```bash
cd /workspace/EtlReverseEngineering && python -m pytest tests/ -x -v --tb=short
```

Smoke test -- package is importable:
```bash
cd /workspace/EtlReverseEngineering && python -c "from workflow_engine.models import JobState, Outcome, NodeType, EngineConfig; from workflow_engine.transitions import TRANSITION_TABLE, HAPPY_PATH, NODE_TYPES; from workflow_engine.nodes import Node, StubWorkNode, StubReviewNode, create_node_registry; from workflow_engine.logging import configure_logging; print('All imports OK')"
```
</verification>

<success_criteria>
- All tests in test_models.py, test_transitions.py, test_nodes.py, test_logging.py pass
- HAPPY_PATH has exactly 27 nodes matching the transition table source document
- TRANSITION_TABLE has 27 edges with correct Outcome types per NodeType
- Every node stub has a descriptive docstring
- Deterministic mode traverses happy path without RNG variation
- structlog produces parseable JSON output
- `python -c "import workflow_engine"` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-happy-path-engine/01-01-SUMMARY.md`
</output>
